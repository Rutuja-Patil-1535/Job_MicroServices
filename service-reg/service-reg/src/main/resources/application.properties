spring.application.name=service-reg
server.port=8761
#it is not service so no need to register in registry (no need to registry in itself)
eureka.client.register-with-eureka=false
#it needs to get the information of registry no itself is registry
eureka.client.fetch-registry=false

#config server
#spring.config.import=configserver:http://localhost:8084
# spring.profiles.active=dev
